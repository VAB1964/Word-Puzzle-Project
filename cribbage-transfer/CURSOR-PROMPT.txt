I need you to integrate the attached Cribbage transfer package into this existing Word-Puzzle-Project repository.

Important background:
- This repository deploys VABGames.com through Cloudflare Pages.
- The root page is already a VAB Games selection page.
- WordPuzzle already works at its own route and must not be broken.
- The supplied `cribbage/` folder contains the complete source of the currently working Cribbage Card Room.
- The current Cribbage tile may still point to https://cribbage-game.vincebr.chatgpt.site.
- The desired permanent address is https://vabgames.com/cribbage/.

Safety rules:
1. Inspect the repository structure and its Cloudflare build/output settings before editing.
2. Do not delete, rename, or reorganize the working WordPuzzle files.
3. Do not change Cloudflare credentials, domains, or account settings.
4. Do not rewrite the Cribbage rules or gameplay unless a build-path compatibility fix is necessary.
5. Do not commit, push, or deploy until I explicitly approve it.
6. Preserve the separate live Cribbage site until the VABGames copy is fully verified.

Required result:
1. Make the supplied Cribbage game available at `/cribbage/` in the final Cloudflare Pages output.
2. Change the Cribbage tile on the VABGames home page to use `/cribbage/` rather than the old chatgpt.site URL.
3. Keep the Cribbage `Back to VABGames.com` button working and returning to `https://vabgames.com`.
4. Preserve every existing Cribbage feature, including:
   - three red/blue/green scoring lanes with clear Start and Finish at 121;
   - 2-, 3-, and 4-player modes, with partnerships for four players;
   - opening low-card draw for first crib, including tie-break draws;
   - complete dealing, crib discard, pegging, Go/last-card handling, hand counting, crib reveal, and winner announcement;
   - scrolling pegging history;
   - detailed hand-count combinations and yellow outline around the starter card;
   - visible pegging-card sequence;
   - Easy, Medium, and Hard AI settings;
   - player-specific peg chimes, card/table sounds, mute, and volume controls;
   - red-only markings for hearts and diamonds;
   - responsive desktop, tablet, and mobile layout.

Integration approach:
- First determine whether the repository deploys source files directly or uses a build output directory.
- The supplied game is a small Vite/React project. Run `npm install` and `npm run build` inside the supplied `cribbage/` folder.
- Its Vite configuration uses `base: "./"`, so the generated files are safe under a subdirectory.
- Copy or arrange the generated `cribbage/dist/` CONTENTS—not an extra nested `dist` directory—so the deployed structure contains:
  `/cribbage/index.html`
  `/cribbage/assets/...`
  `/cribbage/favicon.svg`
- If the repository build process recreates its deployment directory, update that build process to build Cribbage and copy its output every time. Do not rely on a one-time manual copy that a later build will erase.
- Keep the editable Cribbage source in the repository in a clearly named source location. If leaving it directly in `/cribbage/` conflicts with the final static route, use a structure such as `/games-src/cribbage/` for source and copy its build output to the published `/cribbage/` route. Choose the least disruptive structure based on the repository you inspect.
- Do not install a second application framework into the repository root unless the existing build truly requires it.

Verification requirements:
1. Run the repository's normal production build.
2. Serve the exact final Cloudflare Pages output locally, not merely the Cribbage Vite dev server.
3. Verify the VAB Games home page loads.
4. Verify the WordPuzzle tile still opens a working WordPuzzle game with no missing WebAssembly, scripts, images, fonts, or audio.
5. Verify the Cribbage tile opens `/cribbage/` in the same site.
6. Verify a browser refresh while already at `/cribbage/` still loads correctly.
7. Verify the Cribbage menu appears and can start a two-player game.
8. Verify the opening draw leads to the first deal and crib selection.
9. Verify the Back to VABGames.com button returns to the selection page.
10. Check the browser console and network panel for errors or missing files on both games.
11. Check desktop and a narrow mobile viewport.

Before making changes, briefly report:
- how this repository is currently built and published;
- where WordPuzzle's source and final files live;
- where you plan to keep Cribbage source;
- how `/cribbage/` will be produced on every build;
- exactly which existing files you expect to change.

After implementing and testing, stop before commit or push and give me:
- every file added or changed;
- the commands you ran;
- the verification results for both games;
- any remaining concern;
- the exact Git commands I should use only after I approve the result.

If the existing repository structure conflicts with these instructions, do not guess or perform a large reorganization. Explain the conflict and propose the smallest safe adjustment first.
